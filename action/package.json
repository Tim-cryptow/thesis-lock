{
  "name": "thesislock-verify-action",
  "version": "1.0.0",
  "description": "GitHub Action to verify a document hash is anchored on Stacks via ThesisLock",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@stacks/transactions": "^7.4.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
