{
  "name": "thesislock",
  "version": "0.1.0",
  "description": "Hash-anchor service for academic and creative documents on the Stacks blockchain.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:report": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\"",
    "prepare": "husky"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@stacks/clarinet-sdk": "^3.17.0",
    "@stacks/transactions": "^7.0.6",
    "@types/node": "^22.15.18",
    "chokidar-cli": "^3.0.0",
    "vitest": "^3.1.3",
    "vitest-environment-clarinet": "^3.0.2"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  }
}
